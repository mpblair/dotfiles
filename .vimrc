" Must...have...pathogen...for...package...management
execute pathogen#infect()

syntax on
filetype plugin indent on	

set nocompatible
set nomodeline

" tab settings
" --how many columns a tab counts for
set tabstop=4
" --how many columns text is indented with reindent operations
set shiftwidth=4
" --how many columns vim uses when tab is hit in insert mode
set softtabstop=4
" --tab in insert mode produces the appropriate number of spaces
set expandtab

" indentation
" --copy indentation from previous line when starting a new line
set autoindent

" line numbering
set number

